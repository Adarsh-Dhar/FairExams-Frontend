import {atom} from "recoil"

export const questionAtom = atom({
    key: "questionAtom",
    default : ""
})

export const option1atom = atom({
    key: "option1atom",
    default : ""
})

export const option2atom = atom({
    key: "option2atom",
    default : ""
})

export const option3atom = atom({
    key: "option3atom",
    default : ""
})

export const option4atom = atom({
    key: "option4atom",
    default : ""
})

export const correctOptionAtom = atom({
    key: "correctOptionAtom",
    default : null
})

